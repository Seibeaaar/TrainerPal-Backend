export enum FitnessRole {
  trainee = "trainee",
  coach = "coach",
}

export enum WorkoutSite {
  online = "online",
  offline = "offline",
}

export enum WorkoutAccessMode {
  private = "private",
  public = "public",
}
